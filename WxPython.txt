¿Qué es y para qué sirve?
WxPython es un toolkit que envuelve la librería wxWidgets, dedicado para la versión 2.7 de Python, cuya principal funcionalidad es desarrollar interfaces gráficas para aplicaciones, ya sean en ventanas, menús, etc.
Cabe destacar que, para la versión 3.0 de Python, se ha desarrollado otro toolkit similar a wxPython, llamado Project Phoenix, con ciertas mejoras.

Principales módulos
wx -> El conjunto de clases que aparece en en el espacio de nombres del main de wx
wx.html -> Este módulo incluye una clase widget para un html genérico, solo soporta HTML y no javascript ni css. Es adecuado para mostrar documentos HTML simples, como la documentación de una aplicación o las páginas de ayuda integradas.
wx.html2 -> Este módulo incluye una clase widget con soporte para los últimos estándares de HTML, javascript y css.
wx.richtext -> Es una implementación de un control de texto capaz de mostrar múltiples estilos e imágenes.
wx.msw -> Este módulo contiene varias clases solo disponibles para interfaces en windows.

Principales widgets
//Crear una ventana (frame) para la aplicación
frame = wx.Frame(None, wx.ID_ANY, "Texto de prueba");

//Crear un panel para la ventana
pnl = wx.Panel(self)

//Crear un menú
menu = wx.Menu()

//Añadir iconos/shortcuts al menú
menu = wx.Menu() //Creamos el menú

icono = wx.MenuItem(menu, APP_EXIT, '&Quit\tCtrl+Q')
icono.SetBitmap(wx.Bitmap('salir.jpeg'))
fileMenu.AppendItem(icono)

//Crear un botón
btn = wx.Button(pnl, label='Close', pos=(20, 30))

//Crear una línea
wx.StaticLine(self, pos=(25, 50), size=(300,1))

//Crear un texto y mostrarlo en un panel de la ventana
txt = '''Lorem ipsum agirretxe lorea, inla um maquinote marea, insu laudrup pilsen lorrea bet aloem'''
pnl = wx.Panel(self) //Creamos el panel

st = wx.StaticText(pnl, label=txt, style=wx.ALIGN_CENTRE)

//Crear checkbox
pnl = wx.Panel(self) //Creamos el panel

wx.CheckBox(pnl, label='Actualidad', pos=(15, 30))
wx.CheckBox(pnl, label='Deporte', pos=(15, 55))
wx.CheckBox(pnl, label='Ocio', pos=(15, 55))
wx.CheckBox(pnl, label='Ocio', pos=(15, 55))

//Crear elementos select (ComboBox)
deportes = ['Futbol', 'Tenis', 'Baloncesto', 'Golf', 'Pádel', 'Natación', 'Ciclismo', 'Atletismo']
cb = wx.ComboBox(pnl, pos=(60, 35), choices=deportes, style=wx.CB_READONLY)

//Crear radio button
pnl = wx.Panel(self) //Creamos el panel
self.rb1 = wx.RadioButton(pnl, label='Hombre', pos=(10, 10), style=wx.RB_GROUP)
self.rb1 = wx.RadioButton(pnl, label='Mujer', pos=(10, 10), style=wx.RB_GROUP)
self.rb1 = wx.RadioButton(pnl, label='¿Juzgas mi género?', pos=(10, 10), style=wx.RB_GROUP)


